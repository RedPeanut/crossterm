import assert from 'assert';
import fs from 'fs';
import os from 'os';
import path from 'path';
import { MainDialogService } from './MainDialogService';

describe('#AppService', function() {

  beforeEach(function() {
  });

  afterEach(function() {
  });

  it('default ', async function() {
    // const dialogService = new MainDialogService();
    // const msgboxRetVal = await dialogService.showMessageBox(null);
    // console.log(msgboxRetVal);
  });
});
