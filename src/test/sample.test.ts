import assert from 'assert';
// import fs from 'fs';

describe('#sample', function() {
  it('default', function() {
    // assert.equal(received, expected); // 느슨한 비교 (사용 지양)
    // assert.strictEqual(received, expected); // 원시값 비교
    // assert.deepStrictEqual(received, expected); // 객체/배열 구조 비교
  });

});
